apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment_backend_db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web_store_backend_db
  template:
    metadata:
      labels:
        app: web_store_backend_db
    spec:
      containers:
        - name: web_store_backend_db
          image: postgres:12.19-alpine3.20
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: backend_db_secrets
                  key: DB_NAME

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: backend_db_secrets
                  key: DB_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: backend_db_secrets
                  key: DB_PASS

          volumeMounts:
            - mountPath: /var/lib/postgres/data
              name: db-data

      volumes:
        - name: db-data
          persistentVolumeClaim:
            claimName: backend-db-pvc